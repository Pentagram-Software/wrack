# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/

# Node
node_modules/
.next/
dist/

# Xcode / Swift
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
DerivedData/
.build/
*.ipa

# macOS
.DS_Store
.AppleDouble

# IDEs
.vscode/
.idea/
*.swp

# Env
.env
.env.local
*.pem
service-account-key.json
# GCP service-account key files — never commit these
*-key.json
telemetry-writer-key.json

# Raspberry Pi edge session runtime (pid files / logs from edge/scripts/start-all.sh)
edge/run/

# Per-device telemetry secrets on the Pi (never commit)
edge/monitoring/system-metrics.env

# Secret Manager scratch files written by cloud/*/setup-*.sh — never commit these
device-tokens.json
grafana-cloud-push-credentials.json

# EV3 telemetry runtime config (contains API keys — never commit)
robot/controller/telemetry_config.py

# mpy-cross build cache (cloned MicroPython source + built binary; see
# robot/controller/scripts/build_mpy_cross.py) — rebuilt/cached by CI, not committed
.mpy-cross-cache/

# Vision model artifacts — export locally with edge/vision/*/export_model.py
# and copy the .onnx to the Pi; never commit weights or ONNX graphs
*.onnx
*.pt
exported_models/

# Claude Code
.claude/settings.json
.claude/settings.local.json
.mcp.json
